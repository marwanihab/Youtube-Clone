let mongoose;_3b5‍.x([["default",()=>_3b5‍.o]]);_3b5‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const userSchema = new (_3b5‍.a("mongoose",mongoose)).Schema({
  
  password: { type: String, required: true },
  username: { type: String, required: true, unique: true },
  createdAt: Date,
  modifiedAt: Date,
  isDeleted: { type: Boolean, default: false },
})

userSchema.pre('save', function (next) {
  const now = new Date()
  if (!this.createdAt) {
    this.createdAt = now
  }
  this.modifiedAt = now
  next()
})

const userModel = _3b5‍.a("mongoose",mongoose).model('User', userSchema)

_3b5‍.d(userModel);
